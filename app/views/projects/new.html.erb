<div class="container mt-3 mb-3">
    <div class="row m-3">   
        <div class="card mb-3">
            <div class="card-body">
                <h1 class="card-title mb-2">Novo Projeto</h1>

                <%= render 'form', project: @project %>

                <%= link_to 'Voltar', projects_path, class:"btn btn-primary btn-sm mt-3" %>
            </div>
        </div>
    </div>
</div>